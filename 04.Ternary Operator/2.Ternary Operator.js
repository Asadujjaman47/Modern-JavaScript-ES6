// ternary operator
var isLoggedin = true;

// var access = isLoggedin ? true : false;
// insted of this, we can use simply:
var access = isLoggedin;

console.log(access);



// another example
var a = 5

// var access = a > 5 ? true : false;
var access = a > 5;

console.log(access);